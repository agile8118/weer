{
  "name": "weer",
  "description": "URL Shortner Application",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "cd packages/server && yarn start",
    "dev": "cd packages/server && yarn run dev",
    "build": "cd packages/web && yarn run build && cd ../styles && yarn run build && cd ../reusable && yarn run build && cd ../common && npm run build"
  },
  "repository": "git@github.com:agile8118/weer.git",
  "author": "Cododev Technology",
  "license": "ISC"
}
