{
  "name": "weer",
  "description": "URL Shortner Application",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "NODE_ENV=development yarn install && yarn build && cd packages/server && yarn seed",
    "seed": "cd packages/server && yarn seed",
    "start": "cd packages/server && yarn prod",
    "dev": "cd packages/server && yarn run dev",
    "build": "cd packages/reusable && yarn run build && cd ../common && npm run build && cd ../web && yarn run build && cd ../styles && yarn run build && cd ../server && yarn run build"
  },
  "repository": "git@github.com:agile8118/weer.git",
  "author": "Cododev Technology",
  "license": "ISC"
}
